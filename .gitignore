# Build & Executables
build/
build-release/
bin/
out/

# demo/bin holds the GDExtension manifest, which is source, next to the compiled
# library, which is not. Git cannot re-include files under an excluded directory, so
# un-ignore the folder first and then filter its contents. Without this the
# .gdextension manifest never reaches the repository and a fresh clone cannot load the
# extension at all.
#
# The .dll used to be tracked here. It is a 7 MB build output that changed on nearly
# every commit, and it accounted for 125 MB of the repository's 95 MB history — more
# than twice what the committed release archives cost. CI builds it and publishes it as
# an artifact, and users get it from Releases, so committing it bought nothing. Build
# before running the demo project; the README says so.
!demo/bin/
demo/bin/*
!demo/bin/*.gdextension
!demo/bin/*.uid
.vs/
*.user
*.userosv
*.sln.docstates

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CMakeLists.txt.user
Makefile
*.cmake
!CMakeLists.txt

# Compiled Objects & Libraries
*.a
*.lib
*.so
*.dylib
*.dll
*.exe
*.obj
*.o
*.pdb

# Godot 4 specifics
.godot/
*.translation
*.import

# Document Source Files & Internal Drafts
*.docx
*.doc
*.pdf
*.tmp

# Local workspace settings & notes
.local/
*.local

# IDE & OS
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# The godot-mcp bridge script, injected into the demo project for automated testing.
demo/mcp_interaction_server.gd
demo/mcp_interaction_server.gd.uid

# Godot copies the extension DLL aside for hot-reload while the editor runs.
demo/bin/~*.dll

# Packaged releases live on GitHub Releases, not in the repository. Seven of them were
# committed at the repo root (~11 MB) duplicating assets already published on their tags.
/quebratsk-engine-*.zip

# Working notes and reference documents that are not part of the project.
*.docx

# Third-party writing skill installed for local use, not part of the project.
.claude/skills/

# What the dock writes when someone presses Save. Generated from games on the machine,
# reproducible from them, and not something to carry in the repository.
demo/imported/

# What packaging a release produces. Rebuilt from source every time.
release_pack/

# The owner's own research. It stays on disk and is used while working here, and it is not
# theirs to publish for them.
docs/goldsrc_source_architecture_guide.xml

# Working instructions for the assistant. What in it is useful to anybody else lives in
# CONTRIBUTING.md; the rest is tooling and stays local.
CLAUDE.md

# A dead 6.7 MB scene that pointed at a script which does not exist. Deleted once and it came
# back through a working tree that got rewritten, so it is named here rather than trusted to
# stay gone.
demo/main.tscn
